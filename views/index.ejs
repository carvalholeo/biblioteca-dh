<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" integrity="sha384-JcKb8q3iqJ61gNV9KGb8thSsNjpSL0n8PARn9HuZOnIxN0hoP+VmmDGMN5t9UJ0Z" crossorigin="anonymous" />
  </head>
  <body class="container">
    <% if (ehUsuario) { %>
      <%- include('./partials/headerLogado') %>
    <% } else { %>
      <%- include('./partials/headerDeslogado') %>
    <% } %>

    <div class="table-responsive">
      <table class="table table-striped table-hover">
        <thead class="thead-dark">
          <tr>
            <th scope="col">#</th>
            <th scope="col">Título</th>
            <th scope="col">Ano de Lançamento</th>
            <th scope="col">Autor</th>
            <th scope="col">Está à venda?</th>
          </tr>
        </thead>

        <tbody>
          <% theBookIsOnTheTable.forEach(livro => { %>
            <tr>
              <th scope="row"><%= livro.id %></th>
              <td><%= livro.titulo %></td>
              <td><%= livro.ano_lancamento %></td>
              <td><%= livro.autor %></td>
              <td><%= livro.disponivel_venda === true ? 'Disponível' : 'Indisponível' %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
    <p>A página acessada anteriormente foi: <%= paginaAnterior %> </p>
    <p>Você está na página: <%= paginaAtual %></p>
    <p>Esta página foi acessada <%= contador %> vezes.</p>

  </body>
</html>
